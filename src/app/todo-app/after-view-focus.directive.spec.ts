import { AfterViewFocusDirective } from './after-view-focus.directive';

describe('AfterViewFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AfterViewFocusDirective();
    expect(directive).toBeTruthy();
  });
});
